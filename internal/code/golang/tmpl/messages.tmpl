//
// Messages
//
{{ range $msg := . }}
type {{ $msg.Name }} struct {
	{{- range $field := $msg.Fields }}
	{{ $field.Name }} {{ $field.Type }} {{ if $field.Tags }}`{{ $field.Tags }}`{{ end }}
	{{- end }}
}
{{ end -}}
