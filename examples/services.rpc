rpc=0.0.1

enum Flag Int32 { 
  NoName = 1 
  WithName 
}

message User { 
  ID: String {
    json = id
  }
  Name: String
  Flag: Flag {
    json = flag
  }
}

service GreetingService {
	Ping(UserId: String)
  GetUsers() => (users: []User)
	StatusStream(userId: String) => (users: stream User)
}